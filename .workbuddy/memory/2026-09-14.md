# 2026-09-14

## 云函数 ai 超时修复（3 秒被掐断）

- 报错：`errCode: -504003 / Invoking task timed out after 3 seconds`（article-generate 生成失败）。
- 根因：云函数的超时**以云端配置为准**，`cloudfunctions/ai/config.json` 只是期望值，
  必须通过**部署**同步。该函数在云端一直是默认 3 秒（首次创建时未带配置，后续只传了代码）。
- 修复：`npm run deploy:cf -- ai`（走开发者工具 CLI）把 config.json 的 `timeout:120` 同步上云。
- 备忘：在 IDE 里右键"上传并部署"若**没勾选"同时更新函数配置"**，config.json 的 timeout 不会生效 —— 这是本次踩的坑。

## tabBar 图标显示成「图片占位符」

- 排查结论：PNG 文件存在、96×96 RGBA、CRC 全对、解压长度精确匹配、图像渲染正常（书本/拼图/人形），
  app.json 路径与 4 个页面注册都正确，无 custom-tab-bar —— **源码与资源侧无任何问题**。
- 发现差异：原 PNG（Pillow 保存）**缺 `sRGB` 块**。已重生成全部 8 张并显式补 `IHDR sRGB IDAT IEND`。
- 新增脚本 `.workbuddy/rebuild_tabbar.py`：从 `theme/icons.js` 读码位（保证与图标字体同源）→
  用 **子集字体** 渲染（**注意：BMP 私有区 U+E000+ 码位只在子集字体里有，用源字体渲染会得到空字形**）→
  保存后再手工在 IHDR 后插入 sRGB 块。
- 待用户验证：清缓存 + 重新编译后的结果。

## 真题练习无内容 —— 方案 3：内联数据云函数（零密钥导入）

### 问题
- 官方开发者工具 CLI 的 `cloud` 子命令**只有 `env` / `functions`**，没有 `storage` / `database`，
  所以没法用"IDE 登录态"跑 `scripts/upload-content.mjs` 那种走 secretId/secretKey 的脚本。
- 本机也没配 `TCB_SECRET_ID/KEY`，云开发控制台手动传 56 个文件 + 建索引表也不现实。

### 方案（已完成并部署）
新建 **`cloudfunctions/contentseed`**，把真题+故事数据内联进函数随代码包上传，由云函数用管理员权限写云存储：
- `scripts/build-content-data.mjs`（新，`npm run content:build`）：读 `../memo-grad/src/data/` 的
  `realExams.json` + `stories.json`，展开成 `[{path, body}]`（与 upload-content.mjs 完全同构），
  gzip+base64 内联为 `contentseed/content-data.js`。**56 个文件**（真题 34 套 + 索引 1 + 故事 20 章 + 索引 1），
  原始 2451KB → gzip 1137KB → base64 1480KB（部署 gzip 后 1.12MB，远低于 10MB 上限）。
- `cloudfunctions/contentseed/index.js`（新）：action = `info` / `ensureCollection` / `import` / `verify` / `clear`。
  `import` 断点续跑（默认 3 秒超时 → 预算 2500ms 主动收手返回 next），幂等（同 path 先删旧索引再写）。
- `cloudfunctions/contentseed/{package.json,config.json}`（新，timeout 60）。
- `pages/seed` 扩展（不新建页面）：加"内容数据（真题/故事）"区块 + 四个按钮
  （④建内容集合 / ⑤导入 / 抽查 / 清空），`seed.js` 加 `refreshContent` 等 4 个方法 +
  `contentTotal/contentIndexed/contentRemaining/contentProgress` 四个 data 字段；`seed.wxss` 加 `.sec-title`。
- 用 `.workbuddy/_sim_contentseed.cjs` 做了**离线全流程模拟**（mock wx-server-sdk）：
  56 文件导入、幂等复跑不重复、verify 通过、2010-e1 结构完整（reading 4 篇+cloze+newType+translation+writing）、
  故事 20 章、clear 干净。
- 已 `npm run deploy:cf -- contentseed` 部署成功。`check_syntax` / `check_deps` 均 0 错 0 缺失。

### 关键经验
- **云函数代码包里的子目录不可靠**（seed 注释里已记：曾报 ENOENT `/var/user/data`），
  内联数据一律放**与 index.js 同级**的 `.js`（`module.exports = {GZ_B64}`），且该目录要有 `package.json`
  声明 CJS，否则被上层 `"type":"module"` 当 ESM 解析，`module.exports` 拿到 undefined。
- 云函数默认超时 3 秒，任何批量写操作都要**自带预算 + 断点续跑**，不能指望平台不掐。

## 环境备忘（本机）
- Bash 工具可用，但 **`ls/mkdir/cat/grep/head/wc/sed/xargs` 全部缺失** —— 目录操作走 Node `fs`，
  内容检索走 Grep/Glob/Read，命令输出走"脚本写文件 + Read"。
- Python venv：`C:\Users\Administrator\.workbuddy\binaries\python\envs\default\Scripts\python.exe`（Pillow 12.3、fonttools）。
- Node：`C:\Users\Administrator\.workbuddy\binaries\node\versions\22.22.2-3\node.exe`。
- 开发者工具 CLI：`C:\Program Files (x86)\Tencent\微信web开发者工具\cli.bat`（须 IDE 在运行）。

## 故事章节只显示译文、不显示英文原文（重要坑）

**现象**：`story-read` 章节正文里，中文译文正常，英文原文整段不渲染。

**排查**：数据侧完全正常 —— 离线实测 `buildBilingualPairs` 42 段英中 1:1 配对无空段，
`markWords` 的 seg 拼接能精确还原原文（431=431），241 个目标词、正则 1888 字符也都没问题。
所以问题 100% 在渲染层。

**根因**：`.sr__en` 是 `<text>` 标签，却被设成 **`display: block`**。
微信的 `text` 是行内元素，只有 `display:inline` 时正常；设成 `block` 后，
**内部「文本 + `wx:for` 生成的多个子 `<text>`」混排会整段不渲染**。
（同页的 `.sr__zh` 也是 `block` 却正常，因为它**没有子节点**、是纯文本 —— 这正好解释了"只有译文能显示"。）

**修复**（两处页面同一模式，一起改了）：
- `pages/story-read/story-read.wxss`、`pages/article-read/article-read.wxss`：
  `.sr__en` / `.ar__en` 由 `display:block` 改 **`display:inline`**，换行交给外层 `.sr__para`（view）。
- `.wxml`：内层 `wx:for` 的 `<text>` 外面包一层 **`<block wx:for>`**
  （避免 `wx:if` + `wx:for` 同时挂在嵌套 text 上），`user-select` 从外层 text 移到内层。

**通用经验（写进约定）**：
- 小程序里 **`<text>` 一律不要设 `display:block`**；需要块级效果就外面套 `<view>`。
- `<text>` 内做"多节点混排"时，用 `<block wx:for>` 包内层 `<text>`，不要直接在外层 text 上挂 `wx:for`。

### 二次修复（第一版没解决）

用户反馈改完后**仍然只显示目标词、英文原文不显示**（且目标词本来就不需要单独列出）。

- 实测 `markWords` 输出：第 1 段 15 个 seg，7 个 `hit:true`（=目标词）、8 个 `hit:false`（=普通文本）。
  **现象规律 = 只有 `hit:true` 的段渲染出来**，普通段全丢。
- 之前那版仍把外层写成 `<text class="sr__en">` 里嵌 `wx:for` 的子 `<text>` —— **text 嵌套 text**
  才是最可疑的点（`hit:false` 时 class 为空串，也可能一并受影响）。
- **最终采用的稳妥结构（推荐作为本项目标准写法）**：
  - 外层改用 **`<view class="sr__en">`**（view 可自由 `display:block`，无 text 嵌套限制）；
  - 内层只放**叶子 `<text>`**，`wx:for` 直接挂在这个 text 上，class 固定带一个基类
    （`class="sr__seg {{item.hit ? 'is-hit' : ''}}"`，**不要出现空 class**）；
  - 基类 `.sr__seg` / `.ar__seg` 显式声明 `font-size/line-height/color`，不依赖继承。
- 同步改了 `article-read`（`.ar__seg` + 外层 view）。
- 教训：**小程序里"文本按关键词切段再拼回"这种需求，外层容器用 `view`、内层叶子用 `text`，
  不要用 text 包 text**。第一版只改 `display` 是没抓住重点。


### 三次修复 —— 真凶终于找到（前两次方向都错了）

用户第三次反馈"还是只显示目标词、不显示原文，目标词无需显示"，于是**停止 CSS 层面的猜测**，
回到数据层用探针实测，一次命中：

```
para[0]  en(typeof=undefined)  zh(typeof=string)  segs(isArray=true,len=15)
```

**真因**：`miniprogram/utils/story.js` 的 `buildSegments()` 只返回 `{ segs, zh }`，
**压根没有 `en` 字段**。WXML 用 `wx:if="{{para.en}}"` 判断是否渲染英文段落，
`para.en` 恒为 `undefined` → **整段英文永远不渲染**。译文那段 `wx:if="{{showTranslation && para.zh}}"`
用的是真实存在的 `zh`，所以译文能显示 —— 现象完全吻合。

也就是说：前两次改 CSS（`display` → `view` 包裹）**方向全错**，真凶在数据构造。
（那次"只有 hit:true 的段渲染"的观察其实是误判：译文分支显示的是 `zh` 本身，
不是被高亮的英文片段。）

**修复**：
- `utils/story.js#buildSegments` 补 `en: p.en || ''`。
- `pages/article-read/article-read.js` 的 `buildPairs` 补 `en: line`，`.map` 补 `en: p.en || ''`
  （同一个坑，一起堵上）。
- 顺手加固 `utils/article.js#markWords`：入参 `plain` 为空/非字符串时先 `String(plain == null ? '' : plain)`，
  且 `list.length === 0 || !text` 时返回 `[{ text, hit: false }]`（原版空串会返回 `text: undefined`）。

**验证（探针，真实数据 `../memo-grad/src/data/stories.json` 前 5 章）**：
```
段落总数: 249
en 非空段落: 249 / 249
segs 拼接还原 en: 249 / 249
```
`check_syntax.cjs` → `ok: true`（js 52 / json 39 / wxml 37 / wxss 40 全 0 错）。

**写进约定的通用教训**：
**构建函数返回的对象字段，必须和 WXML 里 `wx:if` 引用的字段对齐。**
排查"某块内容不显示"时的正确顺序是：**先打印该字段的实际值 → 再怀疑 CSS / 标签结构**。
本次前两轮都跳过数据层直接改样式，白改了两次。

**清理**：临时探针 `.workbuddy/_probe_en.cjs` 用后即删。

### 四次修复 —— 目标词与前一个单词粘连（缺空格）

用户确认英文原文已显示、高亮也生效，但**目标词与前一个单词粘在一起**（如 `theturbulent`）。

**先验证数据层**（吸取上次教训）：实测 `markWords` 输出，空格**完整保留**在非命中片段里，
`segs 拼接 === 原文 → true`。例：
```
0     "Alex had always found "   ← 尾部空格在
1 HIT "peace"
2     " in the library’s dusty corners, ..."  ← 首尾空格都在
```
所以数据层无问题 —— 是**渲染时行内元素间的空格折叠**。

**根因**：每个 seg 是独立 `<text>`（行内元素），片段以空格结尾/开头，而这些空白位于
**两个行内元素之间**。微信渲染层在特定基础库/机型下会折叠这段空格，
而 `hit:true` 的单词紧邻前一段的尾部，于是粘住；普通片段因为两侧都是普通文本，视觉上看不出。

**修复（双保险）**：
- WXML：内层叶子 `<text>` 加 **`space="nbsp"`**，把片段内的空格渲染成不折叠的 `&nbsp;`
  （`story-read.wxml`、`article-read.wxml`）。
- WXSS：`.sr__seg` / `.ar__seg` 显式声明 **`display:inline` + `letter-spacing:normal` +
  `white-space:pre-wrap` + `font-weight:normal`**，避免被其它规则意外改成 flex/block 或加字距；
  `.is-hit` / `.hit` 里再补 `letter-spacing:normal` 覆盖。

**通用教训**：**把连续文本切成多个行内元素再拼回时，元素边界的空白可能在渲染层被折叠** ——
要么给叶子节点加 `space="nbsp"`，要么不用多元素切分（改用富文本 `rich-text`）。
排查顺序仍是：先确认数据里有空格 → 再查渲染层。

**待用户验证**：重新编译后目标词两侧空格是否正常。

### 五次修复 —— 正文向右溢出（上一版 `pre-wrap` 引入的回归）

用户反馈正文**右边超出边界**。这是**上一次修复引入的回归**。

**根因**：上一次为"防空格被折叠"给 `.sr__seg` / `.ar__seg` 加了 **`white-space: pre-wrap`**。
`pre-wrap` 会保留**源码里的换行**，而 WXML 里每个 `<text>` 标签是多行书写的
（`wx:for`、`wx:key`、`class`、`data-word`、`bindtap` 各占一行），
标签内部这些换行+缩进被当成**真实换行**渲染出来，撑出额外断行与不可见空白 → 整段向右溢出。

**修复**：把 seg 的 `white-space` 改回 **`normal`**；并在段落容器
`.sr__en` / `.ar__en` 上补宽度约束：`white-space:normal` + `word-break:break-word` +
`overflow-wrap:break-word` + `overflow:hidden`。

**要点**：词间空格由 `<text space="nbsp">` 负责，**不需要 `pre-wrap`**。
这两者叠加才是错的 —— `space="nbsp"` 已经解决空格折叠，`pre-wrap` 纯属多余且有害。

**通用教训（重要）**：
- **不要在 WXML 多行标签的 `<text>` 上设 `white-space: pre-wrap`** —— 标签内部的源码换行会被渲染。
- 修 bug 时引入新 CSS 属性要谨慎评估副作用；`pre-wrap` 与 `nowrap` 都属于"会改变空白处理"的属性。
- 行内元素拼文本的溢出兜底：容器加 `word-break:break-word` + `overflow-wrap:break-word`。

**待用户验证**：重新编译后正文是否在卡片内正常换行、无向右溢出。

